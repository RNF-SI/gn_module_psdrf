[pytest]
# import-mode=importlib : utilise le package gn_module_psdrf installé (éditable)
# sans manipuler sys.path — évite les collisions d'import sur backend/.
addopts = --import-mode=importlib
testpaths = backend/tests
